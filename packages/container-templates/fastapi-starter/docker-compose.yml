version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///data/app.db
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
